{
    "builds": [{
        "src": "trail_project/wsgi.py",
        "user": "@vercel/python",
        "config": {"maxLambdaSize":"15mb", "runtime":"python3.10"}
    }],

    "routes":[
        {
            "src":"/(.*)",
            "dest":"trail_project/wsgi.py"
        }
    ]
}